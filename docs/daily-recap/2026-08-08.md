# 2026-08-08

- Made Depth EV the default Biome targets view and promoted its community-estimate explanation above the biome cards.
- Set the Delve per-node fossil baseline to current in-play counts: 1 special fossil per biome node, 1 per generic node, 4 per smuggler's cache (was 3/2/5). Assumption help text updated to match — it no longer cites guide figures it isn't using. A logged sample profile still overrides all three.
- Added a prominent city-node boss-loot summary and clearer per-card EV labels without changing the underlying calculations.
- Moved **How to make money** beside **Assumptions** in the Delve toolbar.
- Revalidated Delve's hourly market files on load and put the live single-kill median beside exact boss EV.
- Increased the global navigation and Overview type scale while preserving horizontal mobile navigation. The Overview change-window picker now reuses the shared `.st-seg` control instead of its own smaller one, so it matches every other tab.
- Added 1h/2h market windows and Strat Watcher for up to ten five-scarab-plus-Astrolabe setups, with searchable slots, immediate item-name tooltips, divine-adjusted movement and five-second top-three rotation.
- Replaced the saved-strategy falling strip on the Overview with a full downward panel that repeats the upward layout and is marked by accent colour only.
- All five Overview desks now cycle through their own top three and bottom three every five seconds. Ranking and rotation live in `src/overviewTrends.js` with `scripts/test-overview-trends.mjs` covering them.
- Replaced the remaining Delve marker wording with the more direct fossil-node wording.
- Market families now live in one place (`src/categories.js`), read by both the snapshot script and the app. Adding a family is one entry plus a nav item.
- Added catalogue drift tracking (`src/catalogue.js`, `catalogue.json` per league). New items were already adopted automatically; renames now get followed when a stable poe.watch id confirms them (self-history carries over) and reported as suspects otherwise. Names that curated boss/Delve data prices are flagged as `CURATED NAMES AFFECTED` in the Actions log and summarised in the Overview data-quality strip.
