# 2026-09-12

- Split deployment collection into independent PoE 1/PoE 2 jobs. Validated snapshots are saved every run before site build, with per-game history cache recovery. A failed refresh remains a failed job but allows the other game to publish alongside validated retained data. Missing artifacts or failed checks still block deployment.

- Verified workflow syntax with actionlint; full fixture suite, recovery regression, build and data validation passed. No live fetch or public data rewrite performed. GitHub execution remains to be checked after push.

- Refined shared Vaal controls, keyboard navigation and loading states; compacted the PoE 2 overview boss shortlist and grouped its featured numbers more closely. History merges now reject mixed leagues and keep the newest timestamp, with price/exchange regression coverage.
