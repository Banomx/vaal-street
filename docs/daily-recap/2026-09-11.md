# 2026-09-11

- Finished Popular farms responsive verification: three columns at 1920/1600px, two at 1366/1100px with the sidebar open, and one on small screens. Narrow cards rearrange metrics and market lists without reducing text size. Mobile cards fit at 390px. Build passed.

- Set PoE 2 Popular farms, Exchange and Price tracker time ranges to 24h by default, matching existing PoE 1 defaults. Users can still select other ranges.

- Fixed PoE 2 fresh deployments dropping retained leagues from the manifest (3 → 2 `league-collapse`). Unrefreshed league files remain unchanged; validation stays enabled. Added a mocked fresh-fetch regression for distinct active-league quotes and exact retained history preservation. Fixed Windows module URLs in the subprocess tests.

- Restored readable red for PoE 1 negative movements. Added missing-window explanations and explicit requested hours to PoE 2 partial-window labels. Audited local and deployed history without rewriting data: Forbidden Rites retains 141 timestamps; the 24h view only has two after a roughly 27h publication gap. GitHub scheduled runs failed during the gap and the latest two succeeded.
