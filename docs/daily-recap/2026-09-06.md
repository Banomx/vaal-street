# 2026-09-06

- Infrastructure audit: upgraded Node target to 24 LTS and Vite to 8; the npm
  advisory audit reports no known vulnerabilities after updating the lockfile.
- Added PR code checks, weekly dependency update proposals, scoped deployment
  permissions, execution timeouts and daily 30-day data recovery artifacts.
- Fixed PoE 2 overlap merges dropping item/pair observations and publication
  checks rejecting normal retention compaction. Missing retained observations
  now fail even if new points hide the loss in the total count.
- Games load separately with render-error recovery. Blocked browser storage
  no longer crashes startup; snapshot downloads have a 30-second deadline.
- Documented recovery, scheduling/backup limitations and import ownership checks.
- PoE 2 file manifests now omit exchange files that have not been generated yet.
- Full tests, production build, data validation and game-boundary checks pass.
  Both game workspaces and the PoE 2 exchange passed a production-browser smoke
  check without console errors or warnings. Hosted workflows await rollout.
